syntax = "proto3";

package match;

option go_package = "./match;match";

import "match/global.proto";
import "match/league.proto";

service MathService {
  // 联赛球队
  rpc LeaguesClubs (EmptyPost) returns (LeaguesClubsTree) {}
  // 俱乐部
  rpc LeaguesClub (InfoPost) returns (BoolResponse) {}
}